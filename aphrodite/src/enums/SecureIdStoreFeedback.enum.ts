
/**
 * Contains the what may have gone wrong when using the password storage
 */
 export enum SecureIdStoreErrorFeedback {
     STORE_ERROR,
     DEFAULT_ID_CARD_LIST_ABSENT,
     ID_ABSENT,
     ID_NICKNAME_OCCUPIED,
     SUCCESS
}
